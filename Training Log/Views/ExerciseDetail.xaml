<Page
    x:Class="Training_Log.Views.ExerciseDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Training_Log.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBlock x:Name="lblTitle" Text="New Exercise" 
                   HorizontalAlignment="Center" 
                   FontSize="40" Margin="0,15,0,0" 
                   />

        <StackPanel Margin="0,25,0,0">
            <TextBlock Text="Name Exercise" Margin="30,0,0,5" FontSize="16"/>
            <TextBox x:Name="txtName" 
                    Margin="30,0" 
                    VerticalAlignment="Center" />
        </StackPanel>
    </StackPanel>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="AcceptButton" 
                          Label="Save" 
                          Icon="Accept" Click="AcceptButton_Click" />
            <AppBarButton x:Name="CancelButton" 
                          Label="Cancel" 
                          Icon="Cancel" Click="CancelButton_Click" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
